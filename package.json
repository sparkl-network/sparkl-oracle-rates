{
  "name": "sparkl-oracle-rates",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Off-chain DOT/USD oracle — fetches rates from public APIs and pushes them to RateSetter on an interval.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "node --import tsx src/index.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "viem": "2.48.11",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.3",
    "typescript": "^5"
  }
}
